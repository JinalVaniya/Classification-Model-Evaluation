main.py has all instances for both binary and multiclass datasets.
The Code folder has all py files, while the datasets have both binary and multiclass train and test datasets.
Only executing the main.py file, which is in the Code folder, will provide all charts and the metrics evaluation report.